# =======================================================
# HTTP SERVER CONFIG
# =======================================================
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# Abilita compressione (riduce egress network)
quarkus.http.enable-compression=true
quarkus.http.compress-media-types=application/json,text/plain

# =======================================================
# CORS CONFIG (FIX)
# =======================================================
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:8081,http://localhost:3000,http://localhost:19006,https://aso-music-frontend.vercel.app
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=Content-Disposition
quarkus.http.cors.access-control-max-age=24H
quarkus.http.cors.access-control-allow-credentials=true

# =======================================================
# API BASE PATH
# =======================================================
quarkus.rest.path=/api

# =======================================================
# FIREBASE STORAGE
# =======================================================
firebase.storage.bucket=asomusic-d39c4.appspot.com
firebase.storage.url-expiration-hours=24

quarkus.cache.caffeine."storage-signed-urls".maximum-size=2000
quarkus.cache.caffeine."storage-signed-urls".expire-after-write=23H
firebase.auth.check-revoked-tokens=false